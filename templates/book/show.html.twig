{% extends 'base.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block body %}

    <h1 class="text-2xl font-bold">Ajouter un nouveau livre</h1>

    <br><br>

    <div>

        <span class="font-bold">Etat : </span>{{ book.availability()|raw }}

        <h1 class="font-bold">{{ book.title }}</h1>

        <h2>{{ book.author }}</h2>

        <p>{{ book.synopsis }}</p>

        <span class="font-bold">nombre de page : </span>{{ book.nbPages }}

    </div>

    <div class="flex mt-4 gap-8">
        <a href="{{ path('book.edit', { 'id': book.id }) }}" class="btn btn-primary">Modifier</a>
        <a href="{{ path('app_home') }}" class="btn btn-primary">Retour à la liste</a>
    </div>

{% endblock %}
