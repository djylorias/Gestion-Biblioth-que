<form method="post" action="{{ path('app_subscriber_delete', {'id': subscriber.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscriber.id) }}">
    <button class="border bg-white text-black! hover:bg-black! hover:text-white! font-bold py-2 px-4 rounded hover:cursor-pointer">Delete</button>
</form>
