<form class="flex mt-4" method="post" action="{{ path('app_subscriber_delete', {'id': subscriber.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscriber.id) }}">
    <button class="text-white bg-red-500 hover:bg-red-700 font-bold py-1 px-2 rounded hover:cursor-pointer">Supprimer</button>
</form>
